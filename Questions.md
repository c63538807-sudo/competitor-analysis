# Questions about Excel templates

## 1. 模板格式与字段含义

- `AIC Chat效果竞品对比.xlsx` 中的 `AIC 表现` 字段目前看起来像是用于放截图，还是应该放 AIC 的回答内容？放截图
- 同一模板里的 `AIC 表现评价` 是否只用于填定性评价？如果是，`AIC 表现` 应该重命名为 `AIC 截图` 或 `AIC 回答` 吗？不用，按原本的来
- `竞品1` 后面有一个空白列头，这个空白列实际是什么意思？它是否应该明确为 `竞品1截图`？可以，就这么命名吧
- 同样，`竞品2`、`竞品3` 之后是否也分别需要对应截图列？现在模板显示的列头不统一，是否需要显式写成 `竞品N名称` + `竞品N截图`？可以
- `竞品表现评价` 这一列是否是一个聚合评价字段，还是应拆分成 `竞品1表现评价`、`竞品2表现评价`、`竞品3表现评价`？程序需要综合所有竞品回答，输出一段总体评价（100字以内）。
- `优劣判断` 的比较方式是：AIC 与所有竞品综合比较，还是与每个竞品分别比较？如果存在多个竞品，应该如何明确记录是“竞品优”还是“竞品1优/竞品2优”？优劣判断是AIC所有竞品综合比较后的最终结论。允许的取值仅有：AIC更优、平局、竞品更优、无需指出具体哪一个竞品更优。日报关注是：AIC 是否领先整体竞品。
- `备注/优化建议` 是否只在“平局”或“竞品优”时填写？如果 AIC 优于竞品，是否也允许写备注或改进建议？是的，AIC优就空着

## 2. `竞品分析具体问答.xlsx` 的结构问题

- 该文件的 `免费次数` 和 `使用模型` 是作为全局元数据，还是每个问题都需要填写？如果是每个问题都需要，为什么模板把它们放在独立行而不是列？这是竞品信息，不是每个问题单独填写。例如：ChatGPT：使用模型：GPT-5 免费次数：5 填写一次即可。程序无需按问题重复填写。
- 模板里 `免费次数` 和 `使用模型` 行对 AIC 统一写 `不用填`，这是否意味着 AIC 永远不记录这两个字段？未来程序是否需要支持 AIC 的这些元数据？AIC 永远不填写
- 该模板只包含 `AIC`、`竞品1名字` 和 `竞品2名字`，而 `AIC Chat效果竞品对比.xlsx` 支持 3 个竞品。这里是否需要统一成相同竞品数量？可以统一成3个
- `竞品分析具体问答.xlsx` 的表头是 `问题`、`问题类型（信息问答类、创作生成类、工具类、推理分析类）`、`AIC`、`竞品1名字`、`竞品2名字`。是否应补充 `指向功能` 这一字段，和另一个模板保持一致？可以
- 这个模板中的 `问题类型` 和另一个模板中的 `问题类型` 是否完全等价？如果不等价，允许的取值范围是什么？完全等价，后面有修改再维护

## 3. 两个模板之间的重复与一致性

- 两个模板都记录问题和问题类型，但第二个模板没有 `日期` 和 `指向功能`。是否需要统一这两者，让两个模板都包含相同的基本问题元信息？可以
- 两个模板中的 `AIC` 字段命名不一致：一个是 `AIC 表现`、一个是 `AIC`。我们是否需要统一命名规则？不需要
- 两个模板中竞品数量不一致：一个支持 3 个竞品，另一个只支持 2 个竞品。这是否代表两个模板的使用场景不同，还是模板设计应标准化成同一竞品数量？统一成3个
- 如果 `AIC Chat效果竞品对比.xlsx` 中的 `竞品表现评价` 是聚合字段，第二个模板中是否还需要单独评价列？目前两个模板对竞品评价的粒度不一致。不需要，竞品分析具体问答表是用来记录测试问题的回答具体内容的，AIC Chat效果竞品对比是记录分析评价结果的，这二者功能不同

## 4. 未来开发可能受到影响的点

- 目前 `AIC Chat效果竞品对比.xlsx` 使用的列头结构不明确、截图与名称混杂，可能导致程序自动填表时难以准确匹配字段。是否需要先明确列头语义再开发？不需要
- `竞品分析具体问答.xlsx` 里将元数据放在行而非列，可能会让 JSON 映射变得不清晰。是否应该改成行列一致的结构？不需要
- 两个模板在竞品数量、字段命名和问题元信息上的不一致，可能会导致生成器需要写不同逻辑处理两份模板。是否有必要先确定“标准模板字段集”？不需要
- 文件本身现在是 `.xlsx` 后缀但内容为文本表格，这会影响后续自动化读取和模板校验，需确认文件格式是否正确。我重新替换了一下，现在应该可以了

## 5. 具体需要确认的问题

1. `AIC Chat效果竞品对比.xlsx` 中 `AIC 表现` 是文本回答、截图，还是两者之一？截图
2. 竞品相关列应明确为 `竞品N名称` + `竞品N截图` 吗？可以
3. `竞品表现评价` 是单个聚合评价，还是需拆成每个竞品的独立评价？聚合评价
4. `优劣判断` 是对 AIC 与所有竞品的总体结论，还是应该明确到具体竞品？总体结论
5. `备注/优化建议` 是否在 AIC 优势时也允许填写？不允许
6. `竞品分析具体问答.xlsx` 的 `免费次数` / `使用模型` 是全局字段还是应当按问题填写？全局字段
7. 竞品数量应统一为 2 还是 3 个，还是应该支持可变数量？3个
8. `问题类型` 与 `指向功能` 是否是两个独立字段？它们的取值范围是什么？1. 问题类型（Question Type）

用于描述用户提问属于哪一类能力测试，主要用于竞品分析统计和日报分类。

当前固定取值：

信息问答
创作生成
工具类
推理分析

未来如有新增能力，可扩展新的类型，但应保持分类稳定。

2. 指向功能（Feature）

用于描述AIC 产品内部对应的功能模块或能力方向，主要用于定位产品能力和指导优化。

例如：

问题	问题类型	指向功能
2025诺贝尔物理学奖授予了谁？	信息问答	知识问答
帮我写一份英文简历	创作生成	AI写作
翻译成日语	工具类	AI翻译
总结下面这篇文章	工具类	AI总结
分析为什么销量下降	推理分析	推理分析
9. 是否需要在第二个模板中也补充 `日期` 和 `指向功能`？不需要
10. 是否当前模板本身的实际文件格式应该调整为真实 Excel，而非文本表格？已调整
